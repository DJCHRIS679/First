
for i in range(27):
        print("Hi Jasmine " +str(i+1))

        #from geopy.geocoders import Nominatim
        #nom = Nominatim()
        #to these:

        #from geopy.geocoders import ArcGIS
        #nom = ArcGIS()
        #The rest of the code remains the same.
